# Owner Documentation CR-SST-0132

Fecha: 2026-07-12.

`sst-extension`, `node-auth` y `sst-bend` enlazan `article-semantic-kind`,
`CR-SST-0132`, `INIT-SST-0005` y `SST-62` en sus contratos de
`browser-extension-article-ingestion`, quick-save y routing. La documentación
exige Web explícito y prohíbe inferirlo desde la URL.

