# CR-HPT-0002 — documentación owner

El backend owner registra ahora:

- binding ARDS/SDD `aligned-local`;
- source map enlazado al catálogo, solución, state y request del control plane;
- adopción explícita de `control-plane-link-policy`;
- adopción corregida de `http-qa-harness-policy` con archivo `.http`;
- comandos ejecutables de tests, checker, proceso local y smoke;
- TODOs intactos para auth, scopes, routing, persistencia y producción.

Las rutas owner autoritativas están bajo `backend/specs/`, `backend/docs/`,
`backend/qa/` y `backend/AGENTS.md`.
