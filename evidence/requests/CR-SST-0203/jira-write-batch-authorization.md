# Autorización del lote Jira JIRA-SEC-PREPROD-01

## Alcance autorizado

- Fecha: `2026-08-22`.
- Request de sincronización: `CR-SST-0203`.
- Proyecto: `SST`.
- Issues permitidos: `SST-86`, `SST-89`, `SST-92` y `SST-95`.
- Operaciones permitidas:
  - actualizar descripciones en los cuatro issues;
  - agregar un comentario de reconciliación en cada issue;
  - cambiar únicamente `SST-89` de prioridad `Medium` a `Low`;
  - agregar a `SST-89` las labels aprobadas sin remover las existentes.
- Operaciones prohibidas: crear, borrar, transicionar, reparentar, asignar, cambiar summaries o modificar cualquier otro issue.

## Fuente de autorización

El usuario aprobó explícitamente `JIRA-SEC-PREPROD-01` y pidió además agregar comentarios y descripciones para conservar el contexto en los tickets, aplicando las policies del Sistema.

La autorización es consumible por este lote. No habilita escrituras Jira posteriores.

## Protección de datos

El lote no contiene passwords, verificadores, hashes, JWT, cookies, CSRF, claves, tokens, identificadores de usuario, identificadores cloud ni valores de secretos. Jira permanece como mirror operativo.

