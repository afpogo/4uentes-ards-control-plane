# Autorización del lote Jira JIRA-SEC-PREPROD-02

## Alcance autorizado

- Fecha: `2026-08-22`.
- Request: `CR-SST-0204`.
- Issues: `SST-86`, `SST-89` y `SST-92`.
- Operaciones: reemplazar descripción y agregar un comentario correctivo en cada issue.
- Hechos a reconciliar:
  - `CR-SST-0199` está `done/validated-live`;
  - `CR-SST-0201` está `done/validated-live`;
  - `CR-SST-0200` continúa `running` por signup, continuidad de registro y logout;
  - `CR-SST-0178` continúa `running` como coordinador.
- Operaciones prohibidas: crear, borrar, transicionar, cambiar prioridad, labels, parent, summary, asignación o cualquier otro issue.

## Fuente

El usuario autorizó explícitamente `JIRA-SEC-PREPROD-02 con las operaciones enumeradas`.

La autorización es consumible por una única ejecución de este lote.

## Protección de datos

No se publican ni conservan passwords, verificadores, hashes, tokens, cookies, CSRF, claves, secretos o identificadores privados de conexión.

