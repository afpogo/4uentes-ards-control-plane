# Owner Documentation CR-SST-0131

Fecha: 2026-07-12.

`sst-extension`, `node-auth` y `sst-bend` enlazan `article-semantic-kind`,
`CR-SST-0131`, `INIT-SST-0005` y `SST-61` en sus contratos de
`browser-extension-session-ingestion`, routing y sesiones. La documentación
separa tipo semántico de formato y mantiene el fallback legacy explícito.

