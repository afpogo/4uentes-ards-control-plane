schema_version: "1.0"
phase: 2
id: "CR-SST-0022"
title: "Implement local fake orchestrator handoff adapter for sst-chatbot"
status: planned
source_request: "requests/inbox/CR-SST-0022-local-fake-orchestrator-handoff-adapter.yaml"
created_at: "2026-05-31"
requester: "4uentes"

initial_scope:
  solution: "sst"
  services:
    - sst-chatbot
  include_optional_services: false

impact_analysis:
  required: true
  affected_services:
    - service_id: sst-chatbot
      via: initial_scope.services
      service_kind: agent-runtime
      ards_kind: backend-api
      status: active
  affected_ards:
    - backend-api
  capabilities: []
  risks:
    - id: DIRTY_WORKING_TREE_OBSERVED
      service_id: sst-chatbot
      severity: medium

required_context:
  - catalog/services/sst-chatbot.yaml
  - inventory/evidence/git/sst-chatbot.md
  - solutions/sst.yaml
  - inventory/phase-0.md
  - inventory/phase-0-decision.md
  - AGENTS.md

required_checks:
  - service_id: sst-chatbot
    command: ".\\.venv\\Scripts\\python.exe scripts\\check.py"

risk:
  level: high
  score: 8
  drivers:
    - id: DIRTY_WORKING_TREE_OBSERVED
      service_id: sst-chatbot
      severity: medium
    - id: ORCHESTRATOR_BOUNDARY_SIMULATION
      service_id: sst-chatbot
      severity: high
    - id: IDEMPOTENCY_CONFLICT_SEMANTICS
      service_id: sst-chatbot
      severity: high
    - id: PROPOSAL_ONLY_AUTHORITY_BOUNDARY
      service_id: sst-chatbot
      severity: high

task_weight:
  classification: complex-high-risk-task
  risk_level: high
  drivers:
    - DIRTY_WORKING_TREE_OBSERVED
    - ORCHESTRATOR_BOUNDARY_SIMULATION
    - IDEMPOTENCY_CONFLICT_SEMANTICS
    - PROPOSAL_ONLY_AUTHORITY_BOUNDARY

model_selection:
  policy_ref: "docs/ai/model-selection-policy.md"
  primary_profile: "gpt-5.5"
  fallback_profile: "highest-available-reasoning-profile"
  reason: "Complex high-risk handoff boundary work involving fake orchestrator semantics, idempotency conflicts, and proposal-only authority guarantees."

subagent_deployment_plan:
  required: true
  parallelizable: true
  roles:
    - architecture-reviewer
    - security-contract-reviewer
    - cross-repo-impact-reviewer
    - validation-reviewer
  fallback: "Runtime thread limit prevented subagent deployment; record fallback and run architecture/security/cross-repo/validation review sequentially in the main agent."
  evidence_required: true

execution:
  requires_human_approval: true
  allowed_window: manual
  branch_strategy: one-branch-per-repo

decision:
  status: approved-for-implementation
  approver: "4uentes"

planner_notes:
  - Functional repositories were not modified.
  - Functional repository checks were not executed.
  - Planner heuristic classified the single-service scope as low risk; this planned response was corrected to complex-high-risk-task because fake orchestrator handoff semantics affect execution authority boundaries and idempotency guarantees.
