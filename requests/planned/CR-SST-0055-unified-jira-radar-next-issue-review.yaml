schema_version: "1.0"
phase: 2
id: "CR-SST-0055"
title: "Build unified Jira radar for feature-state and backlog mirror tickets"
status: "planned"
source_request: "requests/inbox/CR-SST-0055-unified-jira-radar-next-issue-review.yaml"
created_at: "2026-06-07"
requester: "4uentes"

initial_scope:
  solution: "sst"
  repository: "4uentes-orchestor"
  include_functional_repositories: false

impact_analysis:
  required: true
  affected_services: []
  affected_ards:
    - control-plane
  capabilities:
    - jira-feature-state-sync
    - jira-backlog-mirror-ticket-policy
    - unified-jira-radar
  risks:
    - id: INCOMPLETE_JIRA_RADAR
      severity: high
    - id: PREMATURE_BACKLOG_SELECTION
      severity: medium

task_weight:
  classification: "long-context-task"
  risk_level: "medium"
  drivers:
    - INCOMPLETE_JIRA_RADAR
    - PREMATURE_BACKLOG_SELECTION
    - JIRA_READ_ONLY_RECONCILIATION

model_selection:
  policy_ref: "docs/ai/model-selection-policy.md"
  primary_profile: "gpt-5.4-fast-high"
  fallback_profile: "highest-available-reasoning-profile"
  reason: "This request reconciles multiple Jira ticket families and corrects a prior recommendation."

subagent_deployment_plan:
  required: true
  parallelizable: false
  roles:
    - ards-sdd-validator
    - implementation-planner
  fallback: "The main agent performs radar and policy review sequentially."
  evidence_required: true

execution_boundaries:
  allowed:
    - "read Jira through MCP"
    - "generate local evidence"
    - "run local validation"
  forbidden:
    - "write Jira"
    - "assign CR-SST automatically"
    - "modify functional repositories"

validation:
  required_checks:
    - "node --check scripts/jira-mcp/backlog-observe.js"
    - "npm run check"
  evidence_required:
    - "unified_radar"
    - "validation_results"

decision:
  status: "approved"
  approver: "4uentes"
  note: "Proceed as read-only radar correction."
