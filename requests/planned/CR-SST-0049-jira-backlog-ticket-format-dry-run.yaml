schema_version: "1.0"
phase: 2
id: "CR-SST-0049"
title: "Define Jira backlog ticket format and dry-run payloads"
status: "planned"
source_request: "requests/inbox/CR-SST-0049-jira-backlog-ticket-format-dry-run.yaml"
created_at: "2026-06-07"
requester: "4uentes"

initial_scope:
  solution: "sst"
  repository: "4uentes-orchestor"
  include_functional_repositories: false

impact_analysis:
  required: true
  affected_services: []
  affected_ards:
    - control-plane
  capabilities:
    - jira-backlog-registry
    - jira-mcp-dry-run
  risks:
    - id: JIRA_BACKLOG_TICKET_FORMAT
      severity: high
    - id: BACKLOG_IDENTITY_GOVERNANCE
      severity: high
    - id: FUTURE_EXTERNAL_WRITE_BOUNDARY
      severity: high

required_context:
  - "requests/done/CR-SST-0047-jira-backlog-registry-and-cr-allocation-policy.yaml"
  - "state/jira-backlog-registry.yaml"
  - "docs/requests/jira-backlog-registry-policy.md"
  - "docs/requests/jira-mcp-deferred-backlog-e2e-policy.md"
  - "package.json"
  - "AGENTS.md"

task_weight:
  classification: "complex-high-risk-task"
  risk_level: "high"
  drivers:
    - JIRA_BACKLOG_TICKET_FORMAT
    - BACKLOG_IDENTITY_GOVERNANCE
    - FUTURE_EXTERNAL_WRITE_BOUNDARY

model_selection:
  policy_ref: "docs/ai/model-selection-policy.md"
  primary_profile: "gpt-5.5"
  fallback_profile: "highest-available-reasoning-profile"
  reason: "This request defines Jira-visible identity for control-plane backlog items before any future external write."

subagent_deployment_plan:
  required: true
  parallelizable: false
  roles:
    - architecture-reviewer
    - security-contract-reviewer
    - validation-reviewer
  fallback: "If subagents or exact model aliases are unavailable, record the limitation and run the same review steps sequentially with the highest available reasoning profile."
  evidence_required: true

implementation_steps:
  - id: ticket-format-policy
    description: "Document summary, labels, description fields, and assignment semantics for Jira backlog tickets."
  - id: dry-run-generator
    description: "Add a command that renders Jira issue payloads from state/jira-backlog-registry.yaml."
  - id: evidence-generation
    description: "Generate markdown and JSON dry-run evidence without Jira writes."
  - id: validation
    description: "Run syntax check and repository validation."

execution_boundaries:
  allowed:
    - "edit control-plane docs"
    - "edit control-plane scripts"
    - "generate local evidence"
    - "run local validation"
  forbidden:
    - "write Jira"
    - "modify functional repositories"
    - "reserve CR-SST numbers for deferred backlog items"
    - "store Jira secrets, tokens, cookies, private URLs, or cloudId in Git"

validation:
  required_checks:
    - "node --check scripts/jira-mcp/generate-backlog-dry-run.js"
    - "npm run jira:mcp:backlog-dry-run -- --request-id CR-SST-0049 --output-dir evidence/requests/CR-SST-0049"
    - "npm run check"
  evidence_required:
    - "backlog_ticket_payload_dry_run"
    - "implementation_summary"
    - "changed_files_summary"
    - "validation_results"

decision:
  status: "approved"
  approver: "4uentes"
  note: "Proceed as dry-run only; no Jira tickets are created by this request."
