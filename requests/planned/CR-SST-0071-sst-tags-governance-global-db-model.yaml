schema_version: "1.0"
phase: 2
id: "CR-SST-0071"
title: "SST Tags Governance - DB model and global scope governance plan"
status: planned
source_request: "requests/inbox/CR-SST-0071-sst-tags-governance-global-db-model.yaml"
created_at: "2026-06-13"
requester: "4uentes"

initial_scope:
  solution: "sst"
  services: []
  include_optional_services: false

impact_analysis:
  required: true
  affected_services: []
  affected_ards: []
  capabilities:
    - "sst-tags-governance"
  risks:
    - id: CONTROL_PLANE_MODEL_RISK
      severity: high

required_context:
  - "AGENTS.md"
  - "state/features/sst-tags-governance.current.yaml"
  - "docs/requests/sst-tags-governance-contract.md"
  - "docs/requests/README.md"
  - "state/00-index.yaml"

required_checks:
  - service_id: "4uentes-orchestor"
    command: "npm run check"

risk:
  level: high
  score: 7
  drivers:
    - id: CONTROL_PLANE_SCOPE
      severity: high

task_weight:
  classification: "complex-high-risk-task"
  risk_level: high
  drivers:
    - "complex contract refactor"
    - "cross-repo future dependency"
    - "data migration planning"

model_selection:
  policy_ref: "docs/ai/model-selection-policy.md"
  primary_profile: "gpt-5.5"
  fallback_profile: "highest-available-reasoning-profile"
  reason: "Plan de modelo y migración contract-first de alta criticidad funcional."

subagent_deployment_plan:
  required: false
  roles: []
  parallelizable: false
  fallback: "Single-agent planning pass is sufficient because this request is control-plane design only."
  evidence_required: true

execution:
  requires_human_approval: true
  allowed_window: manual
  branch_strategy: one-branch-per-repo

validation:
  required_checks:
    - "4uentes-orchestor: npm run check"
  evidence_required:
    - "contract update"
    - "execution notes"
    - "decision log"

decision:
  status: pending
  approver: TODO
