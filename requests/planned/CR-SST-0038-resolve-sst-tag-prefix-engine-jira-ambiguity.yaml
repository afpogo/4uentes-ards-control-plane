schema_version: "1.0"
phase: 2
id: "CR-SST-0038"
title: "Resolve SST Tag Prefix Engine Jira reconciliation ambiguity"
status: planned
source_request: "requests/inbox/CR-SST-0038-resolve-sst-tag-prefix-engine-jira-ambiguity.yaml"
created_at: "2026-06-07"
requester: "4uentes"

initial_scope:
  solution: "sst"
  services: []
  include_optional_services: false

impact_analysis:
  required: true
  affected_services: []
  affected_ards: []
  capabilities:
    - jira-mcp-ticketing
    - jira-backlog-sync-state-machine
    - sst-tag-prefix-engine
  risks:
    - id: DUPLICATE_JIRA_ISSUE
      severity: high
    - id: INCORRECT_EXISTING_ISSUE_UPDATE
      severity: high
    - id: CONTROL_PLANE_JIRA_DRIFT
      severity: medium

control_plane_scope:
  repository: "4uentes-orchestor"
  reason: "This request resolves Jira sync classification inside the control-plane and does not modify functional repositories."

required_context:
  - "evidence/requests/CR-SST-0036/jira-reconciliation-results.json"
  - "evidence/requests/CR-SST-0037/correction-plan-preview.json"
  - "state/features/sst-tag-prefix-engine.current.yaml"
  - "scripts/jira-mcp/doctor.js"

required_checks:
  - service_id: 4uentes-orchestor
    command: "npm run check"
  - service_id: 4uentes-orchestor
    command: "Get-ChildItem scripts\\jira-mcp -Recurse -Filter *.js | ForEach-Object { node --check $_.FullName }"
  - service_id: 4uentes-orchestor
    command: "npm run jira:mcp:doctor -- --request-id CR-SST-0038 --output-dir evidence/requests/CR-SST-0038 --mode read-only"

risk:
  level: high
  score: 7
  drivers:
    - id: DUPLICATE_JIRA_ISSUE
      severity: high
    - id: INCORRECT_EXISTING_ISSUE_UPDATE
      severity: high
    - id: CONTROL_PLANE_JIRA_DRIFT
      severity: medium

task_weight:
  classification: complex-high-risk-task
  risk_level: high
  drivers:
    - DUPLICATE_JIRA_ISSUE
    - EXTERNAL_MCP_CONNECTIVITY
    - CONTROL_PLANE_PROCESS_LIFECYCLE

model_selection:
  policy_ref: "docs/ai/model-selection-policy.md"
  primary_profile: "gpt-5.5"
  fallback_profile: "highest-available-reasoning-profile"
  reason: "The task decides whether a Jira feature-state match is a true duplicate or a missing issue requiring a future create action."

subagent_deployment_plan:
  required: true
  parallelizable: true
  roles:
    - architecture-reviewer
    - security-contract-reviewer
    - validation-reviewer
  fallback: "If subagent tools are unavailable or not explicitly requested, the main agent must perform architecture, write-boundary, and validation review sequentially."
  evidence_required: true

execution:
  requires_human_approval: true
  allowed_window: manual
  branch_strategy: control-plane-only
  read_only_boundary:
    allowed:
      - "inspect existing reconciliation evidence"
      - "inspect doctor correction plan preview"
      - "inspect sst-tag-prefix-engine feature state"
      - "adjust local doctor classification"
      - "write non-secret ambiguity resolution evidence"
      - "write non-secret correction plan preview"
    forbidden:
      - "create Jira issues"
      - "edit Jira issues"
      - "comment on Jira issues"
      - "transition Jira issues"
      - "close Jira issues"
      - "modify functional repositories"
      - "store tokens, cookies, private URLs, cloudId, or OAuth material in Git"

implementation_steps:
  - id: document-ambiguity
    description: "Record why SST-4 is a related issue reference but not an exact match for sst-tag-prefix-engine."
  - id: refine-doctor-classification
    description: "Classify no-exact-summary matches as create candidates when no exact summary match exists."
  - id: rerun-doctor
    description: "Generate CR-SST-0038 doctor evidence and correction plan preview."
  - id: validate
    description: "Run syntax checks and npm run check."

validation:
  required_checks:
    - "npm run check"
    - "Get-ChildItem scripts\\jira-mcp -Recurse -Filter *.js | ForEach-Object { node --check $_.FullName }"
    - "npm run jira:mcp:doctor -- --request-id CR-SST-0038 --output-dir evidence/requests/CR-SST-0038 --mode read-only"
  evidence_required:
    - "ambiguity_resolution"
    - "doctor_summary"
    - "correction_plan_preview"
    - "changed_files_summary"
    - "validation_results"

decision:
  status: pending
  approver: TODO

planner_notes:
  - "No Jira writes are allowed in this request."
  - "The expected result is a future create candidate, not immediate creation."
  - "A later write request must decide whether to apply updates and create the missing issue."
