schema_version: "1.0"
phase: 2
id: "CR-SST-0043"
title: "Define Jira MCP access and sync health contract"
status: planned
source_request: "requests/inbox/CR-SST-0043-jira-mcp-access-sync-health-contract.yaml"
created_at: "2026-06-07"
requester: "4uentes"

initial_scope:
  solution: "sst"
  services: []
  include_optional_services: false

impact_analysis:
  required: true
  affected_services: []
  affected_ards:
    - "feature_state"
    - "request_lifecycle"
    - "jira_backlog_sync_state_machine"
  capabilities:
    - jira-backlog-sync-state-machine
    - jira-mcp-ticketing
    - control-plane-process-lifecycle
  risks:
    - id: JIRA_MCP_PERMISSION_DRIFT
      severity: high
    - id: JIRA_CONTROL_PLANE_AUTHORITY_DRIFT
      severity: high
    - id: EXTERNAL_WRITE_BOUNDARY_CONFUSION
      severity: high
    - id: MISCLASSIFIED_SYNC_HEALTH
      severity: medium

control_plane_scope:
  repository: "4uentes-orchestor"
  reason: "This request defines access and synchronization health contracts only."

required_context:
  - "requests/done/CR-SST-0042-jira-status-transition-proposals-dry-run.yaml"
  - "docs/requests/jira-write-connection-contract.md"
  - "docs/requests/jira-status-event-transition-contract.md"
  - "state/jira-backlog-sync-machine.yaml"

required_checks:
  - service_id: 4uentes-orchestor
    command: "npm run check"

risk:
  level: high
  score: 8
  drivers:
    - id: JIRA_MCP_PERMISSION_DRIFT
      severity: high
    - id: JIRA_CONTROL_PLANE_AUTHORITY_DRIFT
      severity: high
    - id: EXTERNAL_WRITE_BOUNDARY_CONFUSION
      severity: high
    - id: MISCLASSIFIED_SYNC_HEALTH
      severity: medium

task_weight:
  classification: complex-high-risk-task
  risk_level: high
  drivers:
    - JIRA_MCP_ACCESS_CONTRACT
    - JIRA_CONTROL_PLANE_AUTHORITY_DRIFT
    - SYNC_HEALTH_AND_DRIFT_MODEL

model_selection:
  policy_ref: "docs/ai/model-selection-policy.md"
  primary_profile: "gpt-5.5"
  fallback_profile: "highest-available-reasoning-profile"
  reason: "The task defines access boundaries and synchronization health semantics for Jira and control-plane state."

subagent_deployment_plan:
  required: true
  parallelizable: true
  roles:
    - architecture-reviewer
    - security-contract-reviewer
    - validation-reviewer
  fallback: "If subagent tools are unavailable, the main agent must perform architecture, write-boundary, and validation review sequentially."
  evidence_required: true

execution:
  requires_human_approval: false
  allowed_window: local
  branch_strategy: control-plane-only
  read_only_boundary:
    allowed:
      - "inspect local Jira/MCP contracts and evidence"
      - "document MCP access levels"
      - "document sync health and drift states"
      - "write non-secret documentation and evidence"
    forbidden:
      - "connect to Jira"
      - "write Jira issue status"
      - "modify feature_state status"
      - "move request lifecycle from a runtime command"
      - "modify functional repositories"
      - "store tokens, cookies, private URLs, cloudId, or OAuth material in Git"

implementation_steps:
  - id: define-mcp-access-levels
    description: "Define read-only, local proposal, and approved-write access levels."
  - id: define-api-surface
    description: "List MCP/Jira API operations used for project, issue, search, status, and health checks."
  - id: define-sync-health-model
    description: "Define IN_SYNC, drift, conflict, and approval-required statuses."
  - id: define-next-command
    description: "Define the future jira:mcp:sync-health dry-run command boundary."
  - id: validate
    description: "Run npm run check and record evidence."

validation:
  required_checks:
    - "npm run check"
  evidence_required:
    - "mcp_access_sync_health_analysis"
    - "subagent_deployment_evidence"
    - "changed_files_summary"
    - "validation_results"

decision:
  status: pending
  approver: TODO

planner_notes:
  - "CR-SST-0043 does not implement a sync-health command."
  - "The next implementation request should be CR-SST-0044."
  - "MCP read/search may observe Jira; writes remain policy-gated."

