schema_version: "1.0"
phase: 2
id: "CR-SST-0193"
title: "Implement canonical SST user memory persistence and APIs"
status: "planned"
source_request: "requests/inbox/CR-SST-0193-implement-canonical-user-memory-runtime.yaml"
created_at: "2026-08-17"
requester: "4uentes"
summary: >
  Implement the server-canonical write and read boundary for private SST user
  memory in sst-bend after CR-SST-0192 approves the contract.
control_plane_context:
  initiative_id: "INIT-SST-0010"
  jira_role: "mirror"
  jira_source_of_truth: false
  jira_project_key: "SST"
  jira_issue_type: "Tarea"
  jira_epic_key: "SST-105"
  jira_issue_key: "SST-107"
  jira_parent: "SST-105"
  jira_status_observed: "Tareas por hacer"
depends_on: ["CR-SST-0192"]
implementation_scope:
  owner_repo: "sst-bend"
  must_implement:
    - "Reversible migrations and scoped memory-space persistence."
    - "Events, proposals and accepted fact/intention/thread records."
    - "Recall audit metadata and provenance references."
    - "Idempotent internal/authenticated APIs and negative scope tests."
    - "Retention, correction, deletion or tombstone behavior approved by CR-SST-0192."
  must_not_implement:
    - "Direct LLM calls"
    - "Automatic acceptance of raw chat"
    - "Device sync"
    - "Vector storage as source of truth"
execution:
  child_repo_mutation_allowed: false
  future_execution_condition: "Explicit approval, clean worktree, exact file plan, migration rollback and owner-doc plan."
  jira_write_allowed: false
  jira_write_scope: "Create batch completed and authorization consumed."
required_checks:
  - "sst-bend focused memory tests"
  - "sst-bend npm run check"
  - "control-plane npm run check"
planned_jira_mirror:
  summary: "[SST][INIT-SST-0010][CR-SST-0193] Implement canonical user memory runtime"
  description: >
    IntenciÃ³n de avance: implementar en sst-bend la fuente canÃ³nica de memoria
    privada por tenant, account y user. Incluye migraciones reversibles,
    eventos, propuestas, hechos, intenciones, threads, provenance, auditorÃ­a de
    recall, idempotencia, retenciÃ³n y APIs protegidas. Depende de CR-SST-0192.
    No integra todavÃ­a el LLM, la UI ni la rÃ©plica offline. Jira es mirror;
    ARDS/SDD conserva alcance, aprobaciÃ³n y Definition of Done.
decision:
  status: "approved-for-planning"
  approver: "4uentes"
  decided_at: "2026-08-17"
