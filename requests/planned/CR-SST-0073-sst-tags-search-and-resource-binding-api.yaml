schema_version: "1.0"
phase: 2
id: "CR-SST-0073"
title: "SST Tags - Search and resource binding API"
status: planned
source_request: "requests/inbox/CR-SST-0073-sst-tags-search-and-resource-binding-api.yaml"
created_at: "2026-06-13"
requester: "4uentes"

initial_scope:
  solution: "sst"
  services:
    - "sst-bend"
  include_optional_services: false

impact_analysis:
  required: true
  affected_services:
    - service_id: "sst-bend"
      via: "new tag governance API and resource binding endpoints"
      service_kind: "api"
      ards_kind: "backend-api"
      status: "active"
  affected_ards:
    - "backend-api"
  capabilities:
    - "sst-tags-governance"
    - "article-tags"
  risks:
    - id: API_CONTRACT_RISK
      severity: high

required_context:
  - "AGENTS.md"
  - "docs/requests/sst-tags-governance-contract.md"
  - "state/features/sst-tags-governance.current.yaml"
  - "catalog/services/sst-bend.yaml"

required_checks:
  - service_id: "sst-bend"
    command: "npm run check"
  - service_id: "4uentes-orchestor"
    command: "npm run check"

risk:
  level: high
  score: 10
  drivers:
    - id: API_CONTRACT_RISK
      severity: high
    - id: MULTI_SERVICE_RUNTIME_SCOPE
      severity: medium

task_weight:
  classification: "complex-high-risk-task"
  risk_level: high
  drivers:
    - "new API contract"
    - "autocomplete search behavior"
    - "attach/detach semantics"

model_selection:
  policy_ref: "docs/ai/model-selection-policy.md"
  primary_profile: "gpt-5.5"
  fallback_profile: "highest-available-reasoning-profile"
  reason: "API change with structured contract and migration dependencies."

subagent_deployment_plan:
  required: true
  parallelizable: true
  roles:
    - "backend-contract-reviewer"
    - "ards-sdd-validator"
    - "security-contract-reviewer"
  fallback: "If reviewers are unavailable, perform contract/API review sequentially with explicit decision log."
  evidence_required: true

execution:
  requires_human_approval: true
  allowed_window: manual
  branch_strategy: one-branch-per-repo

validation:
  required_checks:
    - "sst-bend: npm run check"
    - "4uentes-orchestor: npm run check"
  evidence_required:
    - "endpoint contract evidence"
    - "search/dedup test notes"

decision:
  status: pending
  approver: TODO
