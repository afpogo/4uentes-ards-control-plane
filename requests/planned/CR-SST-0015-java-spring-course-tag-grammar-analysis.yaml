schema_version: "1.0"
phase: 2
id: "CR-SST-0015"
title: "Analyze complete Java Spring course folder tag grammar"
status: planned
source_request: "requests/inbox/CR-SST-0015-java-spring-course-tag-grammar-analysis.yaml"
created_at: "2026-05-25"
requester: "4uentes"

initial_scope:
  solution: "sst"
  services:
    - sst-bend
    - sst-fend
  include_optional_services: false

impact_analysis:
  required: true
  affected_services:
    - service_id: sst-bend
      via: initial_scope.services
      service_kind: api
      ards_kind: backend-api
      status: active
    - service_id: sst-fend
      via: initial_scope.services
      service_kind: frontend-spa
      ards_kind: frontend-web
      status: active
  affected_ards:
    - backend-api
    - frontend-web
  capabilities: []
  risks:
    - id: TWO_SERVICE_SCOPE
      severity: medium
    - id: DIRTY_WORKING_TREE_OBSERVED
      service_id: sst-bend
      severity: medium
    - id: DIRTY_WORKING_TREE_OBSERVED
      service_id: sst-fend
      severity: medium

required_context:
  - catalog/services/sst-bend.yaml
  - inventory/phase-0.md
  - catalog/services/sst-fend.yaml
  - inventory/evidence/git/sst-fend.md
  - solutions/sst.yaml
  - inventory/phase-0-decision.md
  - AGENTS.md

required_checks:
  - service_id: sst-bend
    command: "npm run check"
  - service_id: sst-fend
    command: "npm run check"

risk:
  level: medium
  score: 3
  drivers:
    - id: TWO_SERVICE_SCOPE
      severity: medium
    - id: DIRTY_WORKING_TREE_OBSERVED
      service_id: sst-bend
      severity: medium
    - id: DIRTY_WORKING_TREE_OBSERVED
      service_id: sst-fend
      severity: medium

execution:
  requires_human_approval: true
  allowed_window: manual
  branch_strategy: one-branch-per-repo

decision:
  status: pending
  approver: TODO

planner_notes:
  - Functional repositories were not modified.
  - Functional repository checks were not executed.
