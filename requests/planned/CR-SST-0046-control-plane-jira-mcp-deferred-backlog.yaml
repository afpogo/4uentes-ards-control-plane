schema_version: "1.0"
phase: 2
id: "CR-SST-0046"
title: "Define deferred Jira MCP control-plane backlog and E2E evidence policy"
status: planned
source_request: "requests/inbox/CR-SST-0046-control-plane-jira-mcp-deferred-backlog.yaml"
created_at: "2026-06-07"
requester: "4uentes"

initial_scope:
  solution: "sst"
  services: []
  include_optional_services: false

impact_analysis:
  required: true
  affected_services: []
  affected_ards:
    - "feature_state"
    - "request_lifecycle"
    - "jira_backlog_sync_state_machine"
  capabilities:
    - jira-backlog-sync-state-machine
    - jira-mcp-ticketing
    - control-plane-process-lifecycle
  risks:
    - id: UNSYNCHRONIZED_DEFERRED_WORK
      severity: high
    - id: JIRA_CONTROL_PLANE_TRACEABILITY_GAP
      severity: high
    - id: FUTURE_WRITE_WITHOUT_E2E_EVIDENCE
      severity: high

control_plane_scope:
  repository: "4uentes-orchestor"
  reason: "This request records deferred backlog and evidence policy only."

required_context:
  - "requests/done/CR-SST-0045-jira-status-signal-approval-intake-flow.yaml"
  - "requests/done/CR-SST-0044-jira-sync-health-dry-run.yaml"
  - "docs/requests/jira-mcp-access-sync-health-contract.md"
  - "docs/requests/jira-status-signal-approval-intake-flow.md"

required_checks:
  - service_id: 4uentes-orchestor
    command: "npm run check"

risk:
  level: high
  score: 7
  drivers:
    - id: UNSYNCHRONIZED_DEFERRED_WORK
      severity: high
    - id: JIRA_CONTROL_PLANE_TRACEABILITY_GAP
      severity: high
    - id: FUTURE_WRITE_WITHOUT_E2E_EVIDENCE
      severity: high

task_weight:
  classification: complex-high-risk-task
  risk_level: high
  drivers:
    - CONTROL_PLANE_BACKLOG_GOVERNANCE
    - JIRA_MCP_E2E_EVIDENCE_POLICY
    - FUTURE_EXTERNAL_WRITE_BOUNDARY

model_selection:
  policy_ref: "docs/ai/model-selection-policy.md"
  primary_profile: "gpt-5.5"
  fallback_profile: "highest-available-reasoning-profile"
  reason: "The task defines backlog governance and evidence requirements for future Jira/MCP/API/SST synchronization work."

subagent_deployment_plan:
  required: true
  parallelizable: true
  roles:
    - architecture-reviewer
    - security-contract-reviewer
    - validation-reviewer
  fallback: "If subagent tools are unavailable, the main agent must perform architecture, write-boundary, and validation review sequentially."
  evidence_required: true

execution:
  requires_human_approval: false
  allowed_window: local
  branch_strategy: control-plane-only
  read_only_boundary:
    allowed:
      - "inspect local requests and evidence"
      - "define deferred backlog"
      - "define E2E evidence policy"
      - "write non-secret documentation and evidence"
    forbidden:
      - "connect to Jira"
      - "create Jira issues"
      - "write Jira issue status"
      - "modify feature_state status"
      - "modify functional repositories"
      - "store tokens, cookies, private URLs, cloudId, or OAuth material in Git"

implementation_steps:
  - id: confirm-sequence
    description: "Confirm CR-SST-0045 is occupied and use CR-SST-0046 for backlog governance."
  - id: define-deferred-backlog
    description: "List pending Jira/MCP/API/SST tasks with stable backlog IDs and priorities."
  - id: define-e2e-evidence-policy
    description: "Define required evidence before future tasks are synchronized to Jira."
  - id: validate
    description: "Run npm run check and record evidence."

validation:
  required_checks:
    - "npm run check"
  evidence_required:
    - "deferred_backlog"
    - "e2e_evidence_policy"
    - "changed_files_summary"
    - "validation_results"

decision:
  status: pending
  approver: TODO

planner_notes:
  - "This request does not create Jira tickets."
  - "Future Jira tickets must be created through the existing policy and MCP/write gates."
  - "Each deferred backlog item must receive its own CR-SST before implementation."
