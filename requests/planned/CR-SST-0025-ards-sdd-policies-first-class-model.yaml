schema_version: "1.0"
phase: 2
id: "CR-SST-0025"
title: "Promote policies as first-class ARDS/SDD components"
status: planned
source_request: "requests/inbox/CR-SST-0025-ards-sdd-policies-first-class-model.yaml"
created_at: "2026-06-02"
requester: "4uentes"

initial_scope:
  solution: "sst"
  services:
    - 4uentes-auth
    - sst-fend
    - sst-bend
    - sst-extension
    - sst-chatbot
    - sst-4uentes-infra
  include_optional_services: true

impact_analysis:
  required: true
  affected_services:
    - service_id: 4uentes-auth
      service_kind: shared-auth-provider
      ards_kind: shared-auth-provider
      status: active
    - service_id: sst-fend
      service_kind: frontend-spa
      ards_kind: frontend-web
      status: active
    - service_id: sst-bend
      service_kind: api
      ards_kind: backend-api
      status: active
    - service_id: sst-extension
      service_kind: browser-extension
      ards_kind: frontend-extension
      status: optional-active
    - service_id: sst-chatbot
      service_kind: agent-runtime
      ards_kind: backend-api
      status: active
    - service_id: sst-4uentes-infra
      service_kind: gitops-infrastructure
      ards_kind: infra-gitops
      status: active
  affected_ards:
    - control-plane
    - shared-auth-provider
    - frontend-web
    - backend-api
    - frontend-extension
    - infra-gitops
  capabilities:
    - policy-unification
    - policy-state-linkage
  risks:
    - id: ARDS_SDD_COMPONENT_MODEL_CHANGE
      severity: high
    - id: CORE_HANDOFF_BOUNDARY
      severity: high
    - id: CHILD_REPO_ADOPTION_CONTRACT
      severity: high

required_context:
  - "AGENTS.md"
  - "specs/00-index.yaml"
  - "specs/capabilities/inbound/00-index.yaml"
  - "specs/states/00-index.yaml"
  - "state/capability-links.yaml"
  - "evidence/requests/CR-SST-0024/policy-model.md"
  - "docs/idioma-markdown.md"
  - "docs/ai/model-selection-policy.md"

required_checks:
  - service_id: 4uentes-orchestor
    command: "npm run check"

risk:
  level: high
  score: 8
  drivers:
    - id: ARDS_SDD_COMPONENT_MODEL_CHANGE
      severity: high
    - id: CORE_HANDOFF_BOUNDARY
      severity: high
    - id: CHILD_REPO_ADOPTION_CONTRACT
      severity: high

task_weight:
  classification: complex-high-risk-task
  risk_level: high
  drivers:
    - ARDS_SDD_COMPONENT_MODEL_CHANGE
    - CORE_HANDOFF_BOUNDARY
    - CHILD_REPO_ADOPTION_CONTRACT

model_selection:
  policy_ref: "docs/ai/model-selection-policy.md"
  primary_profile: "gpt-5.5"
  fallback_profile: "highest-available-reasoning-profile"
  reason: "Complex high-risk governance modeling work that makes policies first-class ARDS/SDD components next to capabilities and states."

subagent_deployment_plan:
  required: true
  parallelizable: true
  roles:
    - architecture-reviewer
    - cross-repo-impact-reviewer
    - validation-reviewer
  fallback: "Subagent tools require explicit user delegation in this runtime. No explicit delegation was requested, so the main agent performs architecture, cross-repo, and validation reviews sequentially."
  evidence_required: true

execution:
  requires_human_approval: true
  allowed_window: manual
  branch_strategy: orchestrator-only

decision:
  status: approved-for-orchestrator-design
  approver: "4uentes"

planner_notes:
  - Functional repositories are not modified in this phase.
  - 4uentes-core is not modified from this workflow.
  - This request defines the orchestrator-side model and prepares handoff to core.
