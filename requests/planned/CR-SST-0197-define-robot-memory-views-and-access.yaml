schema_version: "1.0"
phase: 2
id: "CR-SST-0197"
title: "Define capability-scoped robot memory views and access"
status: "planned"
source_request: "requests/inbox/CR-SST-0197-define-robot-memory-views-and-access.yaml"
created_at: "2026-08-17"
requester: "4uentes"
summary: >
  Adopt the original robot personality intention as backend-enforced agent
  profiles and filtered memory views rather than unrestricted storage access.
control_plane_context:
  initiative_id: "INIT-SST-0010"
  jira_role: "mirror"
  jira_source_of_truth: false
  jira_project_key: "SST"
  jira_issue_type: "Tarea"
  jira_epic_key: "SST-105"
  jira_issue_key: "SST-111"
  jira_parent: "SST-105"
  jira_status_observed: "Tareas por hacer"
depends_on: ["CR-SST-0192", "CR-SST-0193", "CR-SST-0194"]
implementation_scope:
  owner_repos: ["4uentes-auth", "sst-bend", "sst-chatbot"]
  must_define:
    - "Human account roles versus robot persona and capabilities."
    - "Allowed memory domains, kinds, sources and visibility per robot profile."
    - "Read, propose and handoff permissions."
    - "Backend filtered view and negative cross-scope tests."
    - "Audit metadata for which robot consumed which memory IDs."
  forbidden:
    - "Robot direct database or filesystem access"
    - "Prompt-controlled privilege escalation"
    - "Replacing owner/member account roles with robot roles"
execution:
  child_repo_mutation_allowed: false
  future_execution_condition: "Approve the security contract and exact owner-repo implementation slices."
  jira_write_allowed: false
  jira_write_scope: "Create batch completed and authorization consumed."
required_checks:
  - "Auth and entitlement contract tests"
  - "Cross-user/account/tenant negative tests"
  - "Owner checks and control-plane npm run check"
planned_jira_mirror:
  summary: "[SST][INIT-SST-0010][CR-SST-0197] Define robot memory views and capability access"
  description: >
    IntenciÃ³n de avance: adoptar robots con personalidad y rol operativo como
    perfiles de capacidades, sin eliminar los roles humanos owner/member. Cada
    robot recibirÃ¡ una vista de memoria filtrada por tenant, account, user,
    dominio, kind y entitlement; podrÃ¡ leer o proponer sÃ³lo lo autorizado y
    nunca acceder directamente a DB o filesystem. Depende de CR-SST-0192,
    CR-SST-0193 y CR-SST-0194. Jira es mirror; el contrato de seguridad precede
    cualquier implementaciÃ³n.
decision:
  status: "approved-for-planning"
  approver: "4uentes"
  decided_at: "2026-08-17"
