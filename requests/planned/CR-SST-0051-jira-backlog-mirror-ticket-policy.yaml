schema_version: "1.0"
phase: 2
id: "CR-SST-0051"
title: "Define Jira backlog mirror ticket policy"
status: "planned"
source_request: "requests/inbox/CR-SST-0051-jira-backlog-mirror-ticket-policy.yaml"
created_at: "2026-06-07"
requester: "4uentes"

initial_scope:
  solution: "sst"
  repository: "4uentes-orchestor"
  include_functional_repositories: false

impact_analysis:
  required: true
  affected_services: []
  affected_ards:
    - control-plane
  capabilities:
    - jira-backlog-registry
    - jira-backlog-mirror-ticket-policy
    - control-plane-request-intake
  risks:
    - id: POLICY_DRIFT
      severity: medium
    - id: BACKLOG_ID_CR_SST_CONFUSION
      severity: high

required_context:
  - "requests/done/CR-SST-0047-jira-backlog-registry-and-cr-allocation-policy.yaml"
  - "requests/done/CR-SST-0049-jira-backlog-ticket-format-dry-run.yaml"
  - "requests/done/CR-SST-0050-jira-backlog-create-writer.yaml"
  - "state/jira-backlog-registry.yaml"
  - "docs/requests/jira-backlog-registry-policy.md"
  - "AGENTS.md"

task_weight:
  classification: "long-context-task"
  risk_level: "medium"
  drivers:
    - POLICY_DRIFT
    - BACKLOG_ID_CR_SST_CONFUSION
    - JIRA_SYNCHRONIZATION_RULE

model_selection:
  policy_ref: "docs/ai/model-selection-policy.md"
  primary_profile: "gpt-5.4-fast-high"
  fallback_profile: "highest-available-reasoning-profile"
  reason: "This request updates policy and synchronization rules across existing control-plane artifacts."

subagent_deployment_plan:
  required: true
  parallelizable: false
  roles:
    - ards-sdd-validator
    - implementation-planner
  fallback: "The main agent performed policy and validation review sequentially."
  evidence_required: true

implementation_steps:
  - id: name-ticket-type
    description: "Define the operational ticket type name for deferred backlog Jira issues."
  - id: intake-rule
    description: "Document when backlog mirror tickets must be created."
  - id: sync-rule
    description: "Document how Jira issue keys and assigned CR-SST fields must be maintained."
  - id: validation
    description: "Run control-plane validation."

execution_boundaries:
  allowed:
    - "edit control-plane policy docs"
    - "create local request evidence"
    - "run local validation"
  forbidden:
    - "write Jira"
    - "modify functional repositories"
    - "assign CR-SST ids to deferred backlog items"

validation:
  required_checks:
    - "npm run check"
  evidence_required:
    - "policy_summary"
    - "changed_files_summary"
    - "validation_results"

decision:
  status: "approved"
  approver: "4uentes"
  note: "Proceed as policy-only update."
