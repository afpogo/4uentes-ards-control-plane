schema_version: "1.0"
phase: 2
id: "CR-SST-0206"
title: "Validate consent-aware chat retention end to end"
status: "planned"
source_request: "requests/inbox/CR-SST-0206-integrated-chat-retention-qa.yaml"
created_at: "2026-08-22"
requester: "4uentes"
depends_on: ["CR-SST-0209", "CR-SST-0204", "CR-SST-0205"]
control_plane_context:
  initiative_id: "INIT-SST-0007"
  parent_request_id: "CR-SST-0208"
  jira_role: "mirror"
  jira_source_of_truth: false
  jira_issue_type: "Subtask"
  jira_issue_key: "SST-117"
  jira_parent_issue_key: "SST-113"
  jira_write_authorized: false
  jira_write_batch_status: "consumed-2026-08-22"
scope:
  target_environment: "development"
  child_repo_mutation_targets: []
  qa_surfaces: ["localhost", "reserved-ngrok"]
risk: {level: "high", score: 8, drivers: ["PRIVACY_EVIDENCE", "MULTI_SESSION", "TTL", "DESTRUCTIVE_ACTION"]}
task_weight: {classification: "complex-high-risk-task", risk_level: "high"}
model_selection: {provider: "codex", resource_level: "normal", primary_profile: "gpt-5.6-sol", reasoning_effort: "max", fallback_profile: "gpt-5.5"}
subagent_deployment_plan: {mode: "sequential-fallback", reason: "Proactive delegation is disabled; the integrated matrix runs sequentially with isolated identities."}
execution:
  requires_human_approval: true
  approval_status: "pending-prerequisites"
  child_repo_mutation_allowed: false
  deployment_allowed: false
  production_mutation_allowed: false
  jira_write_allowed: false
qa_matrix:
  - "Temporary: two active sessions share turns; logout/new future session and TTL expiry do not recover them."
  - "Saved: explicit promotion survives logout, Redis eviction and a second device/session."
  - "Cache: first durable read after eviction misses to PostgreSQL; subsequent read hits Redis without changing authority."
  - "Clear local: browser state clears while saved history remains recoverable on demand."
  - "Finish temporary: volatile data disappears and active sessions receive the terminal event."
  - "Delete from SST: PostgreSQL row and Redis cache disappear and cannot be resurrected by a race."
  - "Security: a second principal cannot enumerate, read, promote or delete another user's conversations."
validation:
  - "Record secret-safe HTTP/realtime evidence with synthetic accounts."
  - "Run owner checks and 4uentes-orchestor npm run check before closure."
acceptance:
  - "Every matrix row passes in localhost and the reserved ngrok development route."
  - "Evidence contains no password, token, cookie, message body or Redis credential."
rollback: "QA performs no direct runtime mutation beyond synthetic test data, which is deleted through the product contract."
decision: {status: "pending-prerequisites", approver: "TODO"}

namespace_reconciliation:
  request_id: "CR-SST-0207"
  source_worktree_alias: "cr-sst-0202-consent-chat-retention"
  source_path: "requests/planned/CR-SST-0206-integrated-chat-retention-qa.yaml"
  publication_status: "ported-with-canonical-identities"
  jira_identity_update_status: "pending-explicit-enumerated-batch"
