schema_version: "1.0"
phase: 2
id: "CR-SST-0042"
title: "Implement Jira status transition proposals dry-run command"
status: planned
source_request: "requests/inbox/CR-SST-0042-jira-status-transition-proposals-dry-run.yaml"
created_at: "2026-06-07"
requester: "4uentes"

initial_scope:
  solution: "sst"
  services: []
  include_optional_services: false

impact_analysis:
  required: true
  affected_services: []
  affected_ards:
    - "feature_state"
    - "jira_backlog_sync_state_machine"
  capabilities:
    - jira-backlog-sync-state-machine
    - jira-mcp-ticketing
    - control-plane-process-lifecycle
  risks:
    - id: JIRA_CONTROL_PLANE_AUTHORITY_DRIFT
      severity: high
    - id: EVIDENCELESS_LOCAL_TRANSITION
      severity: high
    - id: EVENT_REPLAY_OR_DUPLICATE_SIGNAL
      severity: medium

control_plane_scope:
  repository: "4uentes-orchestor"
  reason: "This request implements a local dry-run proposal generator only."

required_context:
  - "requests/done/CR-SST-0041-jira-status-event-transition-proposal-flow.yaml"
  - "docs/requests/jira-status-event-transition-contract.md"
  - "evidence/requests/CR-SST-0040/jira-status-observation-results.json"

required_checks:
  - service_id: 4uentes-orchestor
    command: "node --check scripts/jira-mcp/generate-status-proposals.js"
  - service_id: 4uentes-orchestor
    command: "npm run jira:mcp:status-proposals -- --request-id CR-SST-0042 --input-dir evidence/requests/CR-SST-0040 --output-dir evidence/requests/CR-SST-0042"
  - service_id: 4uentes-orchestor
    command: "npm run check"

risk:
  level: high
  score: 7
  drivers:
    - id: JIRA_CONTROL_PLANE_AUTHORITY_DRIFT
      severity: high
    - id: EVIDENCELESS_LOCAL_TRANSITION
      severity: high
    - id: EVENT_REPLAY_OR_DUPLICATE_SIGNAL
      severity: medium

task_weight:
  classification: complex-high-risk-task
  risk_level: high
  drivers:
    - JIRA_CONTROL_PLANE_AUTHORITY_DRIFT
    - CONTROL_PLANE_PROCESS_LIFECYCLE
    - LOCAL_DRY_RUN_PROPOSAL_GENERATOR

model_selection:
  policy_ref: "docs/ai/model-selection-policy.md"
  primary_profile: "gpt-5.5"
  fallback_profile: "highest-available-reasoning-profile"
  reason: "The task implements the first executable boundary between Jira operational signals and local transition proposals."

subagent_deployment_plan:
  required: true
  parallelizable: true
  roles:
    - architecture-reviewer
    - security-contract-reviewer
    - validation-reviewer
  fallback: "If subagent tools are unavailable, the main agent must perform architecture, write-boundary, and validation review sequentially."
  evidence_required: true

execution:
  requires_human_approval: false
  allowed_window: local
  branch_strategy: control-plane-only
  read_only_boundary:
    allowed:
      - "read local Jira status observation evidence"
      - "read local feature_state files"
      - "write non-secret proposal evidence"
      - "add local dry-run command"
    forbidden:
      - "connect to Jira"
      - "write Jira issue status"
      - "modify feature_state status"
      - "move request lifecycle from the command"
      - "modify functional repositories"
      - "store tokens, cookies, private URLs, cloudId, or OAuth material in Git"

implementation_steps:
  - id: add-command
    description: "Add jira:mcp:status-proposals npm script."
  - id: implement-generator
    description: "Implement local dry-run proposal generation from observation evidence."
  - id: run-dry-run
    description: "Run the command using CR-SST-0040 observations and CR-SST-0042 output evidence."
  - id: validate
    description: "Run JS syntax validation and npm run check."

validation:
  required_checks:
    - "node --check scripts/jira-mcp/generate-status-proposals.js"
    - "npm run jira:mcp:status-proposals -- --request-id CR-SST-0042 --input-dir evidence/requests/CR-SST-0040 --output-dir evidence/requests/CR-SST-0042"
    - "npm run check"
  evidence_required:
    - "jira_status_transition_proposals"
    - "implementation_summary"
    - "subagent_deployment_evidence"
    - "changed_files_summary"
    - "validation_results"

decision:
  status: pending
  approver: TODO

planner_notes:
  - "This command is dry-run only."
  - "The command must not require --connect."
  - "The command must not mutate Jira, feature_state, or request lifecycle."

