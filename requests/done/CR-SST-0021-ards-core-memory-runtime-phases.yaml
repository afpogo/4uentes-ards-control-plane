schema_version: "1.0"
phase: 2
id: "CR-SST-0021"
title: "Divide ARDS core memory runtime into programmable phases"
status: "done"
created_at: "2026-05-31"
completed_at: "2026-05-31"
requester: "4uentes"

implemented_change: >
  Implemented the first provider-agnostic ARDS operational memory runtime in
  sst-chatbot with structured records, deterministic validation, programmable
  phases, in-memory store port, fake provider adapter, visibility candidates,
  and orchestrator handoff payload mapping.

affected_repos:
  - service_id: sst-chatbot
    local_binding: "environments/local/bindings.local.yaml"

task_weight:
  classification: "complex-high-risk-task"
  risk_level: "high"
  drivers:
    - "agent-runtime architecture"
    - "structured memory and evidence contracts"
    - "provider/storage abstraction boundary"
    - "orchestrator handoff contract"

model_selection:
  policy_ref: "docs/ai/model-selection-policy.md"
  primary_profile: "gpt-5.5"
  fallback_profile: "highest-available-reasoning-profile"
  reason: "Runtime architecture and cross-repo handoff contract work."

subagent_deployment_plan:
  required: true
  roles:
    - architecture-reviewer
    - security-contract-reviewer
    - cross-repo-impact-reviewer
    - validation-reviewer
  fallback: "validation-reviewer could not spawn because of runtime thread limit; main agent performed validation review."
  evidence_ref: "evidence/requests/CR-SST-0021/subagent-deployment-evidence.md"

validation:
  required_checks:
    - "npm run check"
    - ".\\.venv\\Scripts\\python.exe scripts\\check.py"
  evidence_refs:
    - "evidence/requests/CR-SST-0021/baseline.md"
    - "evidence/requests/CR-SST-0021/subagent-deployment-evidence.md"
    - "evidence/requests/CR-SST-0021/implementation-summary.md"
    - "evidence/requests/CR-SST-0021/changed-files-summary.md"
    - "evidence/requests/CR-SST-0021/validation-results.md"

decision:
  status: "approved-done"
  approver: "4uentes"
  decided_at: "2026-05-31"
