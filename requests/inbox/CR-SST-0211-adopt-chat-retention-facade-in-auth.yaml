schema_version: "1.0"
phase: 1
id: "CR-SST-0211"
title: "Adopt the SST chat retention facade in 4uentes-auth"
status: "inbox"
created_at: "2026-08-22"
requester: "4uentes"
summary: >
  Add the missing 4uentes-auth BFF adoption lifecycle between the Bend-owned
  chat-retention API and the SST frontend so the SPA never bypasses the
  authenticated facade.
requested_scope:
  initiative_id: "INIT-SST-0007"
  parent_request_id: "CR-SST-0202"
  depends_on: ["CR-SST-0204"]
  coordinated_request_ids: ["CR-SST-0204", "CR-SST-0206", "CR-SST-0207"]
  target_environment: "development"
  producer_repositories: ["sst-bend"]
  owner_repositories: ["4uentes-auth"]
  consumer_repositories: ["sst-fend"]
  intended_capability: "authenticated chat-retention HTTP facade"
architecture_boundary:
  observed_gap: >
    CR-SST-0204 plans list, read, promote, finish and durable-delete semantics
    in sst-bend, while sst-fend is required to consume SST HTTP capabilities
    through 4uentes-auth and the current facade exposes only create, message
    history and delete routes.
  failure_behavior: >
    Do not make sst-fend call sst-bend directly and do not silently expand
    CR-SST-0204 or CR-SST-0206 across the 4uentes-auth ownership boundary.
constraints:
  - "Do not modify any child repository until this request is planned and separately approved for owner execution."
  - "Do not redefine access-token, session-family, CSRF or Socket.IO ownership."
  - "Do not expose sst-bend directly at the public HTTP edge."
  - "Do not add production or deployment scope."
  - "Do not write Jira without a separate enumerated batch authorization."
identity_reservation:
  coordinator_request_id: "CR-SST-0208"
  candidate_id: "CR-SST-0211"
  canonical_ref_observed: "origin/main@8c37223"
  preflight_status: "passed-read-only-candidate-absent"
  reservation_status: "prepared-not-reserved-until-merged"
  preserved_neighbor: "CR-SST-0210 remains conceptually reserved for memory identity and scope"
  evidence_ref: "evidence/requests/CR-SST-0211/namespace-and-boundary-preflight-2026-08-22.md"
planning_context:
  task_weight: "complex-high-risk-task"
  risk_level: "high"
  risk_drivers: ["AUTHORIZATION_BOUNDARY", "CROSS_REPO_CONTRACT", "PRIVACY", "REQUEST_IDENTITY"]
  model_provider: "codex"
  resource_level: "normal"
  resource_source: "default"
  primary_profile: "gpt-5.6-sol"
  reasoning_effort: "max"
  fallback_profile: "gpt-5.5"
  fallback_reasoning_effort: "high"
  subagent_deployment_plan: "none-proactive-delegation-disabled-sensitive-boundary-kept-in-primary-agent"
requested_next_step: >
  Merge this minimal inbox reservation into the canonical ref, refresh that
  ref, then create one clean CR-SST-0211 control-plane worktree for planning.
decision:
  status: "pending-reservation-merge"
  approver: null
  decided_at: null
