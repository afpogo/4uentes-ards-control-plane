schema_version: "1.0"
phase: 2
id: "CR-SST-0079"
title: "SST Tags - Reproducible API QA documentation"
status: inbox
created_at: "2026-06-18"
requester: "4uentes"

summary: >
  Crear documentacion reproducible para validar manualmente los endpoints de
  tags governance de SST-21 sin depender de pasos copiados en chat, colecciones
  externas o estado manual no versionado.

requested_change: >
  Versionar un flujo ejecutable de API QA para `sst-bend` basado en el contrato
  OpenAPI existente y en archivos `.http` parametrizados por variables de
  entorno.

scope:
  solution: "sst"
  services:
    - "sst-bend"
    - "4uentes-orchestor"
  include_optional_services: false

requested_outcome:
  - "Flujo manual reproducible para listar definitions, crear values, validar conflicto, buscar values, bindear tags a recursos y limpiar bindings."
  - "No se hardcodean JWTs ni secretos; se usan variables de entorno."
  - "La documentacion explica que `includeTags` sigue leyendo legacy durante CR-SST-0073."
  - "El contrato OpenAPI queda como fuente formal y el archivo `.http` como prueba ejecutable local."

out_of_scope:
  - "Cambiar runtime de `sst-bend`."
  - "Agregar Swagger UI como dependencia obligatoria."
  - "Migrar readers publicos al modelo global."

next_lifecycle_step: planned
