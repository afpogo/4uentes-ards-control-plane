schema_version: "1.0"
phase: 1
id: "CR-SST-0183"
title: "Complete extension background session coordination"
status: "inbox"
created_at: "2026-08-15"
requester: "4uentes"
business_context: >
  CR-SST-0180 added a shared promise inside the extension session service, but
  the real MV3 build and all entry points have not proved a single background owner.
requested_change: >
  Make the background the exclusive refresh/logout coordinator and verify that
  losing callers cannot clear the winner's session state.
expected_outcome: >
  Concurrent extension surfaces converge on one refresh and logout prevents
  new refresh work before local state is cleared.
