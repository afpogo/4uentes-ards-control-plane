schema_version: "1.0"
phase: 2
id: "CR-SST-0101"
title: "Define sst-extension CredentialedWebSource producer contract"
status: inbox
created_at: "2026-07-02"
requester: "4uentes"

business_context: >
  Once session capture is stable, sst-extension needs a producer contract for
  private browser-session content that can later feed SST preview flows.

requested_change: >
  Define the sst-extension owner contract for CredentialedWebSource in
  browser-session mode without using DictionarySecret plaintext in the client.

expected_outcome: >
  Credentialed browser-session content has a documented producer contract and
  security boundary before runtime integration.
